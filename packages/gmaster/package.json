{
  "name": "gmaster",
  "version": "1.0.0",
  "description": "Game master. Handles all game-related logic - accepts moves by players, checks game rules, determines the winner, etc. An instance of game master is spawned for each running game",
  "private": true,
  "author": "Gleb Varenov",
  "license": "MIT",
  "scripts": {
    "start": "node ./bin/www.js",
    "build": "tsc",
    "dev": "NODE_ENV=development DEBUG=gmaster* concurrently -k \"nodemon --watch bin --watch dist ./bin/www.js\" \"tsc --watch --preserveWatchOutput\"",
    "clean": "rimraf dist",
    "test": "MOCK_DB=1 HASURA_URL=http://localhost:8080/v1/graphql jest",
    "test:db": "HASURA_URL=http://localhost:8080/v1/graphql jest",
    "test:server": "jest",
    "test:server:local": "GMASTER_URL=http://localhost:3000 jest"
  },
  "dependencies": {
    "debug": "~4.2.0",
    "express": "^4.17.1",
    "graphqurl": "^0.3.3",
    "morgan": "^1.10.0",
    "xstate": "^4.10.0"
  },
  "devDependencies": {
    "@trulyacerbic/ttt-apis": "*",
    "@types/express": "^4.17.4",
    "@types/graphql": "^14.5.0",
    "@types/jest": "^26.0.14",
    "@types/morgan": "^1.9.0",
    "@types/superagent": "^4.1.7",
    "@types/supertest": "^2.0.9",
    "@types/ws": "^7.2.5",
    "concurrently": "^5.3.0",
    "jest": "^26.0.1",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "supertest": "^5.0.0",
    "ts-jest": "^26.0.0",
    "typescript": "4.0.3"
  }
}
