{
  "name": "ghost",
  "version": "1.0.0",
  "description": "Game host. Backend module to communicate game between players (via sockets) and game master. An instance of game host is spawned for each game.",
  "author": "Gleb Varenov",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc",
    "start": "cross-env DEBUG=*,-engine*,-express:router,-socket.io-parser node index.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@trulyacerbic/ttt-gamesdb": "^1.0.16",
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "global-singleton": "^0.1.2",
    "isomorphic-unfetch": "^3.0.0",
    "socket.io": "^2.3.0",
    "xstate": "^4.8.0"
  },
  "engines": {
    "node": ">=10"
  },
  "devDependencies": {
    "@types/events": "^3.0.0",
    "@types/graphql": "^14.5.0",
    "rimraf": "^3.0.2",
    "typescript": "3.8.3"
  }
}
