{
  "scripts": {
    "build": "tsc -b packages/gmaster packages/ghost",
    "start:client": "cd packages/test_client && yarn dev"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^3.2.2"
  }
}