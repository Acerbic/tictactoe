{
  "scripts": {
    "build": "lerna run build",
    "build:local": "cross-env PRISMA_URI=localhost:4466 lerna run build",
    "start:client:l": "cd packages/test_client && yarn dev",
    "start:gmaster:l": "cross-env-shell PRISMA_URI=localhost:4466 \"cd packages/gmaster && yarn start\"",
    "start:ghost:l": "cross-env-shell PRISMA_URI=localhost:4466 GMASTER_URI=localhost:3000 \"cd packages/ghost && yarn start\""
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "cross-env": "^7.0.2"
  }
}
